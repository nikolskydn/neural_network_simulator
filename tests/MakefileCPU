CXX=g++-4.9
CXXFLAGS=-std=c++14
SRCS=$(wildcard *.cpp)
TRGS=$(SRCS:%.cpp=%_cpu)

DPNDS=$(wildcard ../lib/solver*.hpp)

all: $(TRGS)

$(TRGS): %_cpu: %.cpp $(DPNDS)
	$(CXX) $(CXXFLAGS) -o $@ $^ -L../lib/impl/cpu -lsolvercpu

clean:
	rm -f $(TRGS)

